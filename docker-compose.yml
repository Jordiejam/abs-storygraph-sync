services:
  abs-storygraph-sync:
    build: .
    image: abs-storygraph-sync:local
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./data:/app/data  # persists accounts, per-user settings, and sync state
    environment:
      PORT: "5465"
      # Optional: enable "Sign in with SSO" for any OIDC provider (e.g. PocketID)
      # OIDC_ISSUER: https://id.example.com
      # OIDC_CLIENT_ID: your_client_id
      # OIDC_CLIENT_SECRET: your_client_secret
      # Only needed if you're behind HTTPS-terminating reverse proxy and OIDC
      # redirect URIs still come out as http:// — see README's OIDC section
      # PUBLIC_URL: https://abs-sync.example.com
