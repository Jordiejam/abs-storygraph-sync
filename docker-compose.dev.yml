services:
  abs-storygraph-sync:
    command: ["flask", "--app", "app", "run", "--host=0.0.0.0", "--port=5465", "--reload"]
    environment:
      READ_ONLY: "true"
    volumes:
      - .:/app  # the whole checkout; ./data still lands at /app/data
